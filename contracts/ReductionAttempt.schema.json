{
  "additionalProperties": false,
  "properties": {
    "transform": {
      "maxLength": 600,
      "title": "Transform",
      "type": "string"
    },
    "parent_hash": {
      "pattern": "^[a-f0-9]{64}$",
      "title": "Parent Hash",
      "type": "string"
    },
    "child_hash": {
      "pattern": "^[a-f0-9]{64}$",
      "title": "Child Hash",
      "type": "string"
    },
    "trial_ids": {
      "items": {
        "maxLength": 128,
        "minLength": 1,
        "pattern": "^[A-Za-z0-9][A-Za-z0-9_.:-]*$",
        "type": "string"
      },
      "maxItems": 3,
      "title": "Trial Ids",
      "type": "array"
    },
    "retained": {
      "title": "Retained",
      "type": "boolean"
    },
    "reason": {
      "maxLength": 600,
      "title": "Reason",
      "type": "string"
    }
  },
  "required": [
    "transform",
    "parent_hash",
    "child_hash",
    "trial_ids",
    "retained",
    "reason"
  ],
  "title": "ReductionAttempt",
  "type": "object"
}
